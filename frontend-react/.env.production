# Set this to your actual deployed backend URL before building for
# production (`npm run build` picks up .env.production automatically).
# Left as a placeholder rather than a guessed Railway/Vercel/etc URL —
# fill in wherever this actually gets deployed.
VITE_API_BASE_URL=http://localhost:8000/api
