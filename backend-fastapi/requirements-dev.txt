-r requirements.txt

# Added for the test suite in tests/ — wasn't here before because there
# was no test suite before. httpx is required by FastAPI's TestClient
# in recent versions even though nothing imports it directly. Pinned
# for the same reproducibility reasons as requirements.txt itself.
pytest==9.1.1
httpx==0.28.1
