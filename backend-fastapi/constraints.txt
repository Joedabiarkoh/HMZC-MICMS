# Full transitive dependency closure for requirements.txt/requirements-dev.txt
# — pins every package pip would otherwise resolve on its own (fastapi's
# own dependency on starlette, uvicorn[standard]'s on httptools/
# websockets/watchfiles, etc.), not just the ones this app imports
# directly. Without this, requirements.txt's own version pins only
# constrain the top-level packages; anything pulled in underneath them
# could still drift to a new, unverified release on a future rebuild —
# the same supply-chain/reproducibility gap the top-level pins were
# added to close, one level down.
#
# Generated via `pip freeze` from a clean venv with ONLY
# requirements.txt + requirements-dev.txt installed (nothing else on
# the machine), then verified by running the full test suite (22
# passed) against exactly this set. Install with:
#   pip install -r requirements.txt -c constraints.txt
# (Dockerfile does this automatically.) Regenerate the same way after
# deliberately bumping a version in requirements.txt — see that file's
# own comment.
Mako==1.4.1
MarkupSafe==3.0.3
PyYAML==6.0.3
Pygments==2.21.0
SQLAlchemy==2.0.54
alembic==1.20.0
annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.15.1
bcrypt==4.0.1
boto3==1.43.96
botocore==1.43.96
certifi==2026.7.22
cffi==2.1.1
charset-normalizer==3.5.1
click==8.5.0
colorama==0.4.6
cryptography==50.0.1
dnspython==2.8.0
ecdsa==0.19.2
email-validator==2.3.0
fastapi==0.141.1
greenlet==3.5.6
h11==0.16.0
httpcore==1.0.9
httptools==0.8.0
httpx==0.28.1
idna==3.19
iniconfig==2.3.0
jmespath==1.1.0
packaging==26.3
passlib==1.7.4
pillow==12.3.0
pluggy==1.6.0
psycopg2-binary==2.9.13
pyasn1==0.6.4
pycparser==3.0
pydantic==2.13.5
pydantic-settings==2.15.0
pydantic_core==2.46.5
pypdf==6.19.0
pytest==9.1.1
python-dateutil==2.9.0.post0
python-dotenv==1.2.3
python-jose==3.5.0
python-multipart==0.0.32
qrcode==8.2
reportlab==5.0.1
rsa==4.9.1
s3transfer==0.19.2
sentry-sdk==2.69.2
six==1.17.0
starlette==1.6.0
typing-inspection==0.4.4
typing_extensions==4.16.0
urllib3==2.8.0
uvicorn==0.53.0
watchfiles==1.2.0
websockets==17.1
